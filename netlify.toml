[build]
  command = ""
  publish = "."

[functions]
  directory = "testimony/netlify/functions"
  node_bundler = "esbuild"

# Optional: Redirect rules for cleaner URLs
[[redirects]]
  from = "/api/submit-testimony"
  to = "/.netlify/functions/submit-testimony"
  status = 200